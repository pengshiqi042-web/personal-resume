# 个人简历网站部署指南

将你的个人简历网站部署到互联网，让其他人可以通过网址访问。

## 方法一：使用 Vercel（推荐 - 最简单）

Vercel 是最简单的部署方式，支持一键部署，并且提供免费的域名。

### 步骤：

#### 1. 初始化 Git 仓库（如果还没有）

```bash
git init
git add .
git commit -m "Initial commit"
```

#### 2. 创建 GitHub 仓库并推送代码

1. 访问 [GitHub](https://github.com) 并登录
2. 点击右上角 "+" 号，选择 "New repository"
3. 输入仓库名称（如：portfolio-website）
4. 选择 Public 或 Private
5. **不要**勾选 "Initialize this repository with a README"
6. 点击 "Create repository"

然后推送代码：

```bash
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

#### 3. 在 Vercel 部署

1. 访问 [Vercel](https://vercel.com)
2. 使用 GitHub 账号登录
3. 点击 "Add New..." → "Project"
4. 选择你刚刚创建的 GitHub 仓库
5. 配置项目：
   - Framework Preset: 选择 "Vite"
   - Build Command: `npm run build`（通常会自动识别）
   - Output Directory: `dist`（通常会自动识别）
6. 点击 "Deploy"
7. 等待部署完成（通常1-2分钟）
8. 部署完成后，你会获得一个网址，例如：`https://your-project.vercel.app`

#### 4. 自定义域名（可选）

如果你有自己的域名：
1. 在 Vercel 项目页面点击 "Settings"
2. 选择 "Domains"
3. 输入你的域名并按照说明配置 DNS

---

## 方法二：使用 Netlify

### 步骤：

#### 1-2. 同 Vercel 步骤 1-2（初始化 Git 并推送到 GitHub）

#### 3. 在 Netlify 部署

1. 访问 [Netlify](https://www.netlify.com)
2. 使用 GitHub 账号登录
3. 点击 "Add new site" → "Import an existing project"
4. 选择 GitHub，然后选择你的仓库
5. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
6. 点击 "Deploy site"
7. 等待部署完成
8. 你会获得一个网址，例如：`https://random-name-123.netlify.app`

---

## 方法三：使用 GitHub Pages（适合静态网站）

### 步骤：

#### 1. 安装 gh-pages

```bash
npm install --save-dev gh-pages
```

#### 2. 修改 package.json

在 `package.json` 的 `scripts` 部分添加：

```json
"scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy": "vite build && gh-pages -d dist"
}
```

#### 3. 修改 vite.config.js

```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/仓库名/' // 如果你的仓库名是 portfolio-website，就写 '/portfolio-website/'
})
```

#### 4. 部署

```bash
npm run deploy
```

#### 5. 启用 GitHub Pages

1. 在 GitHub 仓库页面，点击 "Settings"
2. 在左侧菜单找到 "Pages"
3. 在 Source 下拉菜单选择 "gh-pages" 分支
4. 点击 "Save"
5. 等待几分钟，访问：`https://你的用户名.github.io/仓库名/`

---

## 推荐使用 Vercel 的原因

1. ✅ **最简单**：只需连接 GitHub，一键部署
2. ✅ **自动部署**：每次推送到 GitHub 都会自动重新部署
3. ✅ **免费域名**：提供 `.vercel.app` 域名
4. ✅ **速度快**：全球 CDN 加速
5. ✅ **零配置**：自动识别 Vite 项目配置

---

## 部署后的更新

部署后，每次你想更新网站内容：

1. 修改代码
2. 提交更改：
   ```bash
   git add .
   git commit -m "更新内容"
   git push
   ```
3. Vercel/Netlify 会自动检测更改并重新部署（通常1-2分钟）

---

## 常见问题

### Q: 部署后图片不显示怎么办？
A: 确保图片文件在 `public` 文件夹中，并且路径以 `/` 开头（如 `/image.png`）

### Q: 页面刷新后 404？
A: 如果使用 GitHub Pages，需要配置 `base` 路径。Vercel 和 Netlify 通常不需要额外配置。

### Q: 如何查看部署日志？
A: 在 Vercel/Netlify 的项目页面可以查看部署日志和构建错误。

---

**推荐：直接使用 Vercel，最简单快速！** 🚀


