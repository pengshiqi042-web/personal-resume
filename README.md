# 个人作品集网站

一个现代化的个人作品集网站，用于展示项目、技能和经验。

## 功能特性

- 🎨 现代化的 UI 设计
- 📱 完全响应式布局
- 🚀 快速加载和流畅动画
- 📂 项目分类和筛选
- 🔍 项目详情模态框
- 🎯 技能展示
- 📄 个人简介页面

## 技术栈

- **React 18** - UI 框架
- **React Router** - 路由管理
- **Vite** - 构建工具
- **CSS3** - 样式和动画

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

在浏览器中打开 `http://localhost:5173` 查看网站。

### 构建生产版本

```bash
npm run build
```

构建文件将输出到 `dist` 目录。

### 预览生产版本

```bash
npm run preview
```

## 项目结构

```
├── src/
│   ├── components/      # 可复用组件
│   │   ├── Navbar.jsx
│   │   └── Navbar.css
│   ├── pages/          # 页面组件
│   │   ├── Home.jsx
│   │   ├── Projects.jsx
│   │   ├── About.jsx
│   │   └── *.css
│   ├── data/           # 数据文件
│   │   └── projects.js
│   ├── App.jsx         # 主应用组件
│   ├── App.css
│   ├── main.jsx        # 入口文件
│   └── index.css       # 全局样式
├── index.html
├── package.json
└── vite.config.js
```

## 自定义内容

### 修改项目数据

编辑 `src/data/projects.js` 文件来添加、修改或删除项目：

```javascript
{
  id: 1,
  title: '项目标题',
  description: '项目描述',
  technologies: ['技术1', '技术2'],
  category: '分类',
  details: '详细描述',
  features: ['功能1', '功能2']
}
```

### 修改个人信息

编辑 `src/pages/About.jsx` 文件来更新个人信息、技能和联系方式。

### 修改样式

- 全局样式变量在 `src/index.css` 的 `:root` 中定义
- 各组件样式在对应的 `.css` 文件中

## 部署

### Vercel

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 自动部署完成

### Netlify

1. 将代码推送到 GitHub
2. 在 [Netlify](https://netlify.com) 导入项目
3. 构建命令: `npm run build`
4. 发布目录: `dist`

### GitHub Pages

1. 安装 `gh-pages`: `npm install --save-dev gh-pages`
2. 在 `package.json` 中添加部署脚本
3. 运行 `npm run deploy`

## 浏览器支持

- Chrome (最新)
- Firefox (最新)
- Safari (最新)
- Edge (最新)

## 许可证

MIT License





